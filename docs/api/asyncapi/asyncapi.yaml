asyncapi: 3.0.0
info:
  title: Realtime Chat WebSocket API
  version: 1.0.0

servers:
  default:
    host: app.com
    protocol: ws
    pathname: /ws/v1

channels:
  wsChannel:
    $ref: './channels/ws-channel.yaml'

components:
  messages:
    $ref: './components/messages.yaml'

  schemas:
    $ref: '../schema.yaml'

  securitySchemes:
    $ref: './components/security.yaml'